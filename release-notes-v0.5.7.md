# Hesi v0.5.7

## 托盘（tray）健壮性修复

基于 v0.5.6，聚焦托盘启动器 `tray.js` 的两个体验问题，纯 Node 端改动，**无需重建前端**。

### 1. CDP 模式「双页」修复
- 点「打开（CDP 模式）」会启动两个页面的问题已解决。
- 根因：CDP 浏览器用持久化 `data/cdp-profile`，强杀退出后 Chrome 自动恢复上次会话页，叠加启动 URL 形成双页。
- 修复：启动前清空旧 profile 会话状态（每次全新空白 profile 只开单页）+ 重复启动守卫 + `--new-window`。

### 2. 退出即释放服务端口
- 退出托盘（菜单「退出」或直接关 `tray.bat` 窗口）时，4264 服务端口现在**必定释放**，不再留下孤儿 server 进程。
- 修复：`stopServer()` 改为三重保险且同步执行——按已知子进程 pid 杀 → 按 `data/hesi-server.pid` 文件兜底 → **端口级兜底**（`netstat` 定位占用 4264 的监听进程强杀）。无论 server 是否已成为孤儿，端口都释放。
- 配套的 CDP 调试端口（9222）也在退出时被同步关闭。

## 升级说明
- 源码已推送，dev 模式 `node tray.js` 直接生效。
- SEA 便携版需重新打包构建才包含本版 tray 改动（前端无需变动）。
- 版本号：0.5.6 → 0.5.7（运行时 `self.js` 读取，无需重建前端 bundle）。
